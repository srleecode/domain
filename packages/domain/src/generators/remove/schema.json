{
  "$schema": "http://json-schema.org/schema",
  "id": "RemoveGenerator",
  "title": "Remove Generator Schema",
  "type": "object",
  "description": "removes domain including libraries and cypress projects",
  "properties": {
    "application": {
      "type": "string",
      "description": "Application that the domain belongs to",
      "x-prompt": "What application will the domain being removed be under?"
    },
    "domain": {
      "type": "string",
      "description": "Name of the domaint that will be removed",
      "x-prompt": "What is the name of the domain?"
    }
  }
}
