{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "remove-cypress-project",
  "title": "Remove cypress project",
  "type": "object",
  "properties": {
    "application": {
      "type": "string",
      "description": "Application of the domain you are creating the cypress cypress project for",
      "x-prompt": "What is application of the domain you are creating the cypress cypress project for?"
    },
    "domain": {
      "type": "string",
      "description": "Name of the domain",
      "x-prompt": "What is the domain you are creating the cypress cypress project for?"
    },
    "projectType": {
      "description": "The type of project",
      "type": "string",
      "enum": ["e2e", "storybook"],
      "default": "e2e"
    }
  }
}
