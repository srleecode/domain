{
  "$schema": "http://json-schema.org/draft-07/schema",
  "id": "add-e2e-project",
  "title": "Add e2e project",
  "type": "object",
  "properties": {
    "application": {
      "type": "string",
      "description": "Application of the domain you are creating the e2e cypress project for",
      "alias": "a",
      "x-prompt": "What is application of the domain you are creating the e2e cypress project for?"
    },
    "domain": {
      "type": "string",
      "description": "Name of the domain",
      "alias": "d",
      "x-prompt": "What is the domain you are creating the e2e cypress project for?"
    },
    "linter": {
      "description": "The tool to use for running lint checks.",
      "type": "string",
      "enum": ["eslint", "tslint"],
      "default": "eslint"
    }
  },
  "required": ["application", "domain"]
}
