declare global {
  namespace Cypress {
    interface Chainable<Subject> {
      <%= camelize(name) %>(): Chainable<<%= classify(name) %>>
    }
  }
}

export class <%= classify(name) %> {
    constructor(public element: JQuery) {}
}

Cypress.Commands.add('<%= camelize(name) %>', () => Cypress.get('write selector here').then(e => new <%= classify(name) %>(e)));
