{
  "$schema": "http://json-schema.org/schema",
  "$id": "MoveGenerator",
  "title": "Move Generator Schema",
  "cli": "nx",
  "type": "object",
  "description": "Moves domain including libraries and cypress projects",
  "properties": {
    "folder": {
      "type": "string",
      "description": "Path to the grouping folder relative to the root of the workspace that will be moved",
      "x-prompt": "What is the path for the grouping folder relative to the root of the workspace that will be moved?"
    },
    "destination": {
      "type": "string",
      "description": "Path to move the grouping folder relative to the root of the workspaceo to",
      "x-prompt": "What is the path for the grouping folder relative to the root of the workspace that will be moved?"
    }
  },
  "required": ["folder"]
}
