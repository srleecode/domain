{
  "$schema": "http://json-schema.org/schema",
  "$id": "CreateMockFileGenerator",
  "title": "Create Mock File Generator Schema",
  "type": "object",
  "description": "Creates a file for mocks",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "The name of the project the component test files will be added to",
      "x-prompt": "What is the project name that you want to add the component test files to?"
    },
    "mockFileName": {
      "type": "string",
      "description": "The name of the mock file",
      "x-prompt": "What is the name that you want for the mock file? For example, test-example will create text-example.mock.ts"
    }
  },
  "required": [
    "projectName",
    "mockFileName"
  ]
}
