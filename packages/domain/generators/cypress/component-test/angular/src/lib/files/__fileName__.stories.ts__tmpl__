import { moduleMetadata, Story, Meta } from '@storybook/angular';
import { CommonModule } from '@angular/common';
import { action } from '@storybook/addon-actions';
import { <% if(!isDirective) { %><%= className %>,<% } %> <%= moduleName %> } from './<%= fileName %>.<%= type %>';
<% if(isDirective) { %>import { <%= className %> } from './test.component';<% } %>

export default {
  component: <%= className %>,
  title: '<%= storybookTitle %>',
} as Meta;

const Template: Story<<%= className %>> = () => ({
  component: <%= className %>,
  moduleMetadata({
    <% if(isDirective) { %>declarations: [<%= className %>],<% } %>
    imports: [ ...GLOBAL_MOUNT_OPTIONS.imports, <%= moduleName %> ]
  })
});

export const Default = Template.bind({});